# Address Booking program

Command line application for managing addresses of users, writen in C

## Starting

- Clone the repository
- Run `make` to compile the program
- Run `./bin/main`

## Usage

- Choose from a set of actions
- To perform an actions input a numeric value from available actions in the console
- To show available actions, input `0` to the console